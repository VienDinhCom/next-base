import React from 'react';
import classes from './VideoPostForm.module.css';

interface Props {}

function VideoPostForm(props: Props) {
  return <p className={classes.root}>VideoPostForm works!</p>;
}

VideoPostForm.defaultProps = {};

export default VideoPostForm;
