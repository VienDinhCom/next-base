import React from 'react';
import classes from './Register.module.css';

interface Props {}

function Register(props: Props) {
  return <p className={classes.root}>Register works!</p>;
}

Register.defaultProps = {};

export default Register;
